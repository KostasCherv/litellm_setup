[project]
name = "litellm-setup"
version = "0.1.0"
description = "LiteLLM proxy with monitoring"
requires-python = ">=3.10"
dependencies = [
    "litellm[proxy]==1.81.13",
]

[tool.uv]
# dev-dependencies optional
